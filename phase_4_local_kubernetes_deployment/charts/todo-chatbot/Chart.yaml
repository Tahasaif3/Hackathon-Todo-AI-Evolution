apiVersion: v2
name: todo-chatbot
description: Helm chart for Todo Chatbot application deployment
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  # No external dependencies for local Minikube deployment
  # Add cloud-specific dependencies here when moving to production
