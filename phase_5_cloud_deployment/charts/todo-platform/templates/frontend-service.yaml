apiVersion: v1
kind: Service
metadata:
  name: {{ include "todo-platform.fullname" . }}-frontend
  labels:
    {{- include "todo-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    {{- include "todo-platform.selectorLabels" . | nindent 4 }}
    app: frontend